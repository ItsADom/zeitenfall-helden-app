{
  "name": "helden-app",
  "private": true,
  "version": "0.1.4",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w client",
    "start": "npm run build -w client && npm run start -w server",
    "start:secure": "cross-env SECURE_COOKIES=1 npm run start",
    "test": "npm run test -w shared",
    "seed": "npm run seed -w server",
    "seed:dummy": "npm run seed:dummy -w server",
    "seed:testuser": "npm run seed:testuser -w server"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  },
  "allowScripts": {
    "better-sqlite3@12.11.1": true,
    "esbuild@0.28.1": true,
    "esbuild@0.25.12": true
  }
}
